export type SpotifyTrack = {
  artist: string;
  title: string;
  uri: string;
  albumUrl: string;
};

export type SpotifySearchResult = {
  artist: string;
  title: string;
  uri: string;
  albumUrl: string;
};
